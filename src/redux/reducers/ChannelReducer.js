import { SET_ROOM_ID } from '../types';

const initialState = {
  roomID: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
